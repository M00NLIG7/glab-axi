{
  "schema": "glab-axi/issue-edit-consumer-contract/v2",
  "surface": "best-effort exact-identity GitLab issue edit",
  "provider": "gitlab",
  "reference": {
    "repository": "https://github.com/kunchenguid/gh-axi",
    "commit": "2bffd9a5b60ded64d6c9851683b27a480173a7ee",
    "source": "src/commands/issue.ts:editIssue",
    "covered_fields": ["title", "body", "add-label", "remove-label"],
    "remaining_gaps": ["assignees", "milestone", "attachments", "GitHub organization issue types"]
  },
  "glab_axi": {
    "required_envelope": "glab-axi/ux-v1",
    "required_backend": "official-glab",
    "required_upstream_version": "1.112.0"
  },
  "planned_invocation": {
    "argv": ["issue", "edit", "{iid}", "--repo", "{nested_project}", "--hostname", "{host}", "--expected-url", "{canonical_issue_url}", "--expected-state", "{opened|closed}", "--expected-updated-at", "{updated_at}", "{field_flags}", "--format", "json"],
    "field_flags": ["--title-file {private_file}", "--description-file {private_file}", "--add-label {exact_name} (repeatable)", "--remove-label {exact_name} (repeatable)"],
    "preview_flag": "--dry-run",
    "required_explicit_inputs": ["iid", "nested_project", "host", "canonical_issue_url", "expected_state", "expected_updated_at", "at_least_one_field_change"],
    "forbidden_fields": ["state_event", "assignee_ids", "milestone_id", "discussion_locked", "confidential", "issue_type", "weight", "epic_id", "health_status"]
  },
  "success_contract": {
    "envelope_schema": "glab-axi/ux-v1",
    "data_schema": "schema/ux-v1/issue-edit.schema.json",
    "actions": ["preview", "unchanged", "updated", "reconciled_update"],
    "outcomes": ["not_applied", "observed_applied"]
  },
  "ambiguity_contract": {
    "error": "ambiguous_update",
    "action": "ambiguous",
    "outcome": "unknown",
    "receipt_schema": "schema/ux-v1/issue-edit.schema.json",
    "retryable": false
  },
  "provider_contract": {
    "project_reads": 1,
    "issue_reads_per_validation": 2,
    "mutation_attempts_maximum": 1,
    "issue_reads_after_attempt": 1,
    "label_catalog": "complete bounded project and ancestor catalog, twice before and once after a write when labels are requested",
    "requested_fields": ["title", "description", "add_labels", "remove_labels"],
    "mutation_target": "validated numeric project ID and issue IID, selected host",
    "provider_precondition": "GitLab cannot enforce an atomic expected revision or numeric label identity on this write. Concurrent edits or label renames between checks and PUT can race; receipts describe observed state, not exclusive authorship.",
    "payload_policy": "only actual changes; comma-separated exact label deltas, never replacement labels or updated_at; scoped label replacement requires explicit removal",
    "description_policy": "reject slash-leading lines, including inside code blocks, to prevent implicit GitLab quick actions",
    "verification": "exact identity, expected state, title, description, full intended label set, requested label identities, non-regressing timestamp; no retry or rollback",
    "bounds": {"preflight_seconds": 20, "mutation_seconds": 15, "verification_seconds": 10, "total_seconds": 45, "pages_per_catalog": 10, "json_page_bytes": 2097152, "operation_bytes": 8388608}
  }
}
