{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://glab-axi.invalid/schema/ux-v1/mr-discussions.schema.json",
  "title": "glab-axi/ux-v1 mr-discussions data",
  "type": "object",
  "additionalProperties": false,
  "required": ["mr", "discussions"],
  "properties": {
    "mr": {"$ref": "#/$defs/mr_identity"},
    "discussions": {
      "type": "array",
      "maxItems": 1000,
      "items": {"$ref": "#/$defs/discussion"}
    }
  },
  "$defs": {
    "project_identity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "full_path", "web_url"],
      "properties": {
        "id": {"type": "integer", "minimum": 1},
        "full_path": {"type": "string", "minLength": 3, "maxLength": 1024},
        "web_url": {"type": "string", "format": "uri", "pattern": "^https://"}
      }
    },
    "mr_identity": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "iid", "project_id", "web_url", "same_project", "source_project", "target_project", "source_branch", "target_branch", "base_sha", "head_sha", "updated_at"],
      "properties": {
        "id": {"type": "integer", "minimum": 1},
        "iid": {"type": "integer", "minimum": 1},
        "project_id": {"type": "integer", "minimum": 1},
        "web_url": {"type": "string", "format": "uri", "pattern": "^https://"},
        "same_project": {"type": "boolean"},
        "source_project": {"$ref": "#/$defs/project_identity"},
        "target_project": {"$ref": "#/$defs/project_identity"},
        "source_branch": {"type": "string", "minLength": 1, "maxLength": 1024},
        "target_branch": {"type": "string", "minLength": 1, "maxLength": 1024},
        "base_sha": {"type": "string", "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$"},
        "head_sha": {"type": "string", "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$"},
        "updated_at": {"type": "string", "format": "date-time"}
      }
    },
    "user": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "username"],
      "properties": {
        "id": {"type": "integer", "minimum": 1},
        "username": {"type": "string", "minLength": 1, "maxLength": 256},
        "name": {"type": "string", "maxLength": 256}
      }
    },
    "line": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "type": {"type": "string", "maxLength": 128},
        "old_line": {"type": "integer", "minimum": 1},
        "new_line": {"type": "integer", "minimum": 1},
        "line_code": {"type": "string", "maxLength": 256}
      },
      "anyOf": [
        {"required": ["old_line"]},
        {"required": ["new_line"]}
      ]
    },
    "line_range": {
      "type": "object",
      "additionalProperties": false,
      "required": ["start", "end"],
      "properties": {
        "start": {"$ref": "#/$defs/line"},
        "end": {"$ref": "#/$defs/line"}
      }
    },
    "position": {
      "type": "object",
      "additionalProperties": false,
      "required": ["position_type"],
      "properties": {
        "position_type": {"type": "string", "minLength": 1, "maxLength": 128},
        "base_sha": {"type": "string", "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$"},
        "start_sha": {"type": "string", "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$"},
        "head_sha": {"type": "string", "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$"},
        "old_path": {"type": "string", "maxLength": 1024},
        "new_path": {"type": "string", "maxLength": 1024},
        "old_line": {"type": "integer", "minimum": 1},
        "new_line": {"type": "integer", "minimum": 1},
        "line_range": {"$ref": "#/$defs/line_range"}
      },
      "anyOf": [
        {"required": ["old_path"]},
        {"required": ["new_path"]}
      ]
    },
    "note": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "author", "created_at", "updated_at", "body", "system", "resolvable", "resolved"],
      "properties": {
        "id": {"type": "integer", "minimum": 1},
        "type": {"type": "string", "maxLength": 128},
        "author": {"$ref": "#/$defs/user"},
        "created_at": {"type": "string", "format": "date-time"},
        "updated_at": {"type": "string", "format": "date-time"},
        "body": {"type": "string", "minLength": 1, "maxLength": 131072},
        "system": {"type": "boolean"},
        "resolvable": {"type": "boolean"},
        "resolved": {"type": "boolean"},
        "resolved_by": {"$ref": "#/$defs/user"},
        "resolved_at": {"type": "string", "format": "date-time"},
        "position": {"$ref": "#/$defs/position"}
      }
    },
    "discussion": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "target_project_id", "merge_request_id", "merge_request_iid", "individual_note", "resolvable", "resolved", "resolution_state", "notes"],
      "properties": {
        "id": {"type": "string", "minLength": 1, "maxLength": 256},
        "target_project_id": {"type": "integer", "minimum": 1},
        "merge_request_id": {"type": "integer", "minimum": 1},
        "merge_request_iid": {"type": "integer", "minimum": 1},
        "individual_note": {"type": "boolean"},
        "resolvable": {"type": "boolean"},
        "resolved": {"type": "boolean"},
        "resolution_state": {"enum": ["not_resolvable", "unresolved", "resolved"]},
        "notes": {
          "type": "array",
          "minItems": 1,
          "maxItems": 1000,
          "items": {"$ref": "#/$defs/note"}
        }
      }
    }
  }
}
